# This makes the namespace safer by default for workloads that forget to set 
# a specific ServiceAccount. Kubernetes supports disabling token automount on 
# a ServiceAccount, and applying it to default is a common hardening step.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: default
  namespace: team-alpha
automountServiceAccountToken: false
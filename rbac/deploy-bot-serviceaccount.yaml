# Add ServiceAccount for deploy-bot
# Note: You can keep automountServiceAccountToken: true here because this bot 
#       does need API access (that’s the whole point of the Role/RoleBinding), 
#       whereas myapp does not and stays false.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: deploy-bot
  namespace: team-alpha
automountServiceAccountToken: true